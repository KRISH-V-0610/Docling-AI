# Lumina AI v8 — fal.ai LTX-2 backend
# Much lighter than v7 — no local GPU dependencies required!
#
# Install:  pip install -r requirements.txt

fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.28.0
python-dotenv==1.0.1
python-multipart==0.0.19
pydantic==2.10.3

# Optional — only needed if you set ANTHROPIC_API_KEY for better prompts
# anthropic>=0.40.0