# Merged dependencies for all four Python services hosted by the unified
# python-ai entrypoint:
#   - backend-chatbot-spellCheck/ (Dockyyy + reconstruct pipeline)
#   - backend-agent-pipeline/     (deep-scan / FormatForge)
#   - backend-agent-fileEditor/   (DocBot DOCX editor)
#   - backend-readme-generator-github/ (GitHub README generator)

# ── Web framework ─────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0
starlette>=0.37
python-dotenv>=1.0.0
python-multipart>=0.0.9
httpx>=0.27

# ── File extraction ───────────────────────────────────────────────────────
PyPDF2
python-docx>=1.1.0
pdfplumber>=0.11.0
Pillow                # image extraction + raster→PNG conversion for figures (Phase B)

# ── Shared JWT verification (services/common/jwt_auth.py) ────────────────
PyJWT>=2.8.0

# ── Agno + Groq LLM stack ────────────────────────────────────────────────
agno>=1.2.0
agno[sqlite]
groq>=0.8.0

# ── MCP server (file-editor's --mcp flag) ────────────────────────────────
mcp

# ── Agno transitive deps not auto-pulled in some envs ────────────────────
sqlalchemy
aiosqlite
ddgs                  # DuckDuckGo search (newer agno renamed from duckduckgo-search)

# ── Citation / bibliography (deepscan CitationEngine + latex_toolkit, Phase H) ─
# citeproc-py was imported by citation_engine.py but never declared — this fixes
# the long-standing silent fallback, and powers references_to_bibtex.
citeproc-py
citeproc-py-styles
bibtexparser
thefuzz              # fuzzy author-name matching in citation_engine (was undeclared)

# ── LaTeX Toolkit (Phase H) — document conversion + export ───────────────────
# pypandoc drives LaTeX→DOCX/MD export (needs the pandoc binary in the image);
# PyMuPDF (fitz) powers PDF→LaTeX ingestion.
pypandoc
PyMuPDF
